@model Smart_ELearning.ViewModels.AccountViewModels.StudentInClassVM
@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
@{ var classId = ViewBag.ClassId;}
<br>
<div class="p-4 border rounded bg-white">
    <div class="row">
        <div class="col-6">
            <h2 class="text-primary">Test for Lesson: @ViewBag.ScheduleName </h2>
            <div>
                <p>
                    <a href="@Url.Action("Index","Class") class=" fas fa-long-arrow-alt-left>Class</a>/
                    <a href="@Url.Action("ClassSchedule","Schedule", new {classId=classId })">Schedule</a>/Test
                </p>
            </div>
        </div>
        <div class="col-6 text-right">
            <a class="btn btn-primary" href="@Url.Action("CreateTestToSchedule","Test",new {scheduleId=ViewBag.ScheduleId})"><i class="fas fa-plus"></i> &nbsp;New Test</a>
        </div>
    </div>
    <br>
    <br>
    <table id="tblData" class="table table-striped table-bordered" style="width:100%">
        <thead class="thead-dark">
            <tr class="table-info">
                <th>Title</th>
                <th>No of Question</th>
                <th></th>
            </tr>
        </thead>
    </table>
</div>
@section Scripts{
    <script>
        var dataTable;
        var classId = @ViewBag.ScheduleId;
        console.log(classId);

        $(document).ready(function () {
            loadDataTable();
        });

        function loadDataTable() {
            dataTable = $('#tblData').DataTable({
                "ajax": {
                    "url": "/user/schedule/getscheduletotest/"+classId,
                    "type": "GET",
                    "dataType": "json",
                    /*"data": JSON.stringify({ id:classId })*/
                },
                "columns": [
                    { "data": "title" },
                    { "data": "numberOfQuestion" },

                    {
                        "data": "id",
                        "render": function (data) {
                            return `
                                <div class="text-center">

                                    <a href="/User/Test/TestQuestion/?testId=${data}" class="btn btn-success text-white" style="cursor:pointer">
                                    <i class="fas fa-edit">View Questions</i>
                                </a>
                                    <a onclick=Delete("/User/Test/Delete/${data}&classId=${classId}") class="btn btn-danger text-white" style="cursor:pointer">
                                        <i class="fas fa-trash-alt"></i>
                                    </a>
                                </div>
                               `;
                        }, "width": "25%"
                    }
                ]
            });
        }

        function Delete(url) {
            swal({
                title: "Are you sure you want to Delete?",
                text: "You will not be able to restore the data!",
                icon: "warning",
                buttons: true,
                dangerMode: true
            }).then((willDelete) => {
                if (willDelete) {
                    $.ajax({
                        type: "DELETE",
                        url: url,
                        success: function (data) {
                            if (data.success) {
                                toastr.success(data.message);
                                dataTable.ajax.reload();
                            }
                            else {
                                toastr.error(data.message);
                            }
                        }
                    });
                }
            });
        }
    </script>
}